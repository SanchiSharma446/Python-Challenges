word = input("Input the string for which you want to count the vowels of:")
vowels = ['a', 'e', 'i', 'o', 'u']

count = 0

for i in range(len(word)):
    if word[i] in vowels:
        count = count + 1
print(f"there are {count} vowels in your word!")
